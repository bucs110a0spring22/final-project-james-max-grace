import time
import pygame
import collision
import sys

pygame.init()

class Stopwatch:

  ## When any of the difficulty buttons are clicked and the game starts
  def startStopwatch(self):
      clock = pygame.time.Clock()


  ## When a collision occurs in the game the timer will stop and be recorded in a variable
  def endStopwatch(self):


  ## When the exit screen comes up, the time will be displayed using turtle.write
  def displayTime(self):
     