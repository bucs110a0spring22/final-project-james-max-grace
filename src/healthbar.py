from src import player, fallingobjects, collision

class Health:
	def decreasehealth(self):
		#draw red rectangle
		#draw green rectangle over
		if collision == True:
			#detele first rectangle and replace with rectangle 1/3 length
			